# 数据库设计文档

> 项目：AI Knowledge Base
> 版本：v4.0 Final
> 更新：2026-07-02

---

## 1. 数据库信息

```
数据库名：ai_knowledge_base
字符集：utf8mb4
排序规则：utf8mb4_general_ci
MySQL 版本：8.x
```

---

## 2. 表清单

| 序号 | 表名 | 说明 |
|:---:|------|------|
| 1 | user | 用户表 |
| 2 | knowledge | 知识库表 |
| 3 | document | 文档表 |

---

## 3. 表结构定义

### 3.1 user — 用户表

```sql
CREATE TABLE `user` (
    `id`          BIGINT       PRIMARY KEY AUTO_INCREMENT  COMMENT '主键',
    `username`    VARCHAR(50)  NOT NULL UNIQUE              COMMENT '用户名',
    `password`    VARCHAR(255) NOT NULL                     COMMENT '密码（BCrypt加密后）',
    `nickname`    VARCHAR(100) DEFAULT NULL                 COMMENT '昵称',
    `create_time` DATETIME     DEFAULT CURRENT_TIMESTAMP   COMMENT '注册时间',
    `update_time` DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|:---:|------|
| id | BIGINT | PK | 自增主键 |
| username | VARCHAR(50) | ✅ | 登录用户名，唯一约束 |
| password | VARCHAR(255) | ✅ | BCrypt 加密后的密文（固定 60 字符，VARCHAR(255) 留余量） |
| nickname | VARCHAR(100) | ❌ | 展示名，不填则显示 username |
| create_time | DATETIME | 自动 | 注册时间 |
| update_time | DATETIME | 自动 | 最后修改时间 |

**索引：**
- `uk_username` — username 字段的 UNIQUE 约束（建表时自动创建）

---

### 3.2 knowledge — 知识库表

```sql
CREATE TABLE `knowledge` (
    `id`          BIGINT        PRIMARY KEY AUTO_INCREMENT  COMMENT '主键',
    `user_id`     BIGINT        NOT NULL                    COMMENT '创建者ID（关联user.id）',
    `name`        VARCHAR(100)  NOT NULL                    COMMENT '知识库名称',
    `description` VARCHAR(500)  DEFAULT NULL                COMMENT '描述',
    `create_time` DATETIME      DEFAULT CURRENT_TIMESTAMP   COMMENT '创建时间',
    `update_time` DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识库表';
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|:---:|------|
| id | BIGINT | PK | 自增主键 |
| user_id | BIGINT | ✅ | 创建者的 user.id，用于数据隔离 |
| name | VARCHAR(100) | ✅ | 知识库名称 |
| description | VARCHAR(500) | ❌ | 知识库描述 |
| create_time | DATETIME | 自动 | 创建时间 |
| update_time | DATETIME | 自动 | 最后修改时间 |

**索引：**
- `idx_user_id` — 按用户查询知识库列表

---

### 3.3 document — 文档表

```sql
CREATE TABLE `document` (
    `id`            BIGINT        PRIMARY KEY AUTO_INCREMENT  COMMENT '主键',
    `knowledge_id`  BIGINT        NOT NULL                    COMMENT '所属知识库ID（关联knowledge.id）',
    `file_name`     VARCHAR(255)  NOT NULL                    COMMENT '原始文件名',
    `file_type`     VARCHAR(20)   NOT NULL                    COMMENT '文件类型：pdf/docx/txt/md',
    `file_size`     BIGINT        NOT NULL                    COMMENT '文件大小（字节）',
    `file_path`     VARCHAR(500)  NOT NULL                    COMMENT '服务器存储路径',
    `status`        TINYINT       DEFAULT 0                   COMMENT '状态：0待处理 1已完成 2失败',
    `error_msg`     VARCHAR(500)  DEFAULT NULL                COMMENT '失败原因',
    `create_time`   DATETIME      DEFAULT CURRENT_TIMESTAMP   COMMENT '上传时间',
    `update_time`   DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_knowledge_id` (`knowledge_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文档表';
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|:---:|------|
| id | BIGINT | PK | 自增主键 |
| knowledge_id | BIGINT | ✅ | 所属知识库的 knowledge.id |
| file_name | VARCHAR(255) | ✅ | 用户上传时的原始文件名 |
| file_type | VARCHAR(20) | ✅ | 后缀名：pdf、docx、txt、md |
| file_size | BIGINT | ✅ | 文件字节数 |
| file_path | VARCHAR(500) | ✅ | 服务器上的存储路径 |
| status | TINYINT | ✅ | 0=待处理，1=已完成，2=失败 |
| error_msg | VARCHAR(500) | ❌ | status=2 时记录失败原因 |
| create_time | DATETIME | 自动 | 上传时间 |
| update_time | DATETIME | 自动 | 最后修改时间 |

**索引：**
- `idx_knowledge_id` — 按知识库查询文档列表

---

## 4. 表关系

```
user  ──1:N──▶  knowledge  ──1:N──▶  document

一个用户可以有多个知识库
一个知识库可以有多篇文档
```

---

## 5. 命名规范

| 项目 | 规范 | 示例 |
|------|------|------|
| 表名 | 小写下划线，单数 | `user`、`knowledge` |
| 字段名 | 小写下划线 | `user_id`、`file_name` |
| 主键 | 固定叫 `id` | BIGINT AUTO_INCREMENT |
| 外键字段 | `{关联表}_id` | `user_id`、`knowledge_id` |
| 索引名 | `idx_{字段名}` | `idx_user_id` |
| 唯一索引 | `uk_{字段名}` | uk_username（UNIQUE约束） |
| 时间字段 | `create_time` / `update_time` | DATETIME，自动填充 |

---

## 6. 完整建表 SQL（可直接执行）

```sql
CREATE DATABASE IF NOT EXISTS ai_knowledge_base DEFAULT CHARSET utf8mb4;
USE ai_knowledge_base;

CREATE TABLE `user` (
    `id`          BIGINT       PRIMARY KEY AUTO_INCREMENT  COMMENT '主键',
    `username`    VARCHAR(50)  NOT NULL UNIQUE              COMMENT '用户名',
    `password`    VARCHAR(255) NOT NULL                     COMMENT '密码（BCrypt）',
    `nickname`    VARCHAR(100) DEFAULT NULL                 COMMENT '昵称',
    `create_time` DATETIME     DEFAULT CURRENT_TIMESTAMP   COMMENT '注册时间',
    `update_time` DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

CREATE TABLE `knowledge` (
    `id`          BIGINT        PRIMARY KEY AUTO_INCREMENT  COMMENT '主键',
    `user_id`     BIGINT        NOT NULL                    COMMENT '创建者ID',
    `name`        VARCHAR(100)  NOT NULL                    COMMENT '知识库名称',
    `description` VARCHAR(500)  DEFAULT NULL                COMMENT '描述',
    `create_time` DATETIME      DEFAULT CURRENT_TIMESTAMP   COMMENT '创建时间',
    `update_time` DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识库表';

CREATE TABLE `document` (
    `id`            BIGINT        PRIMARY KEY AUTO_INCREMENT  COMMENT '主键',
    `knowledge_id`  BIGINT        NOT NULL                    COMMENT '知识库ID',
    `file_name`     VARCHAR(255)  NOT NULL                    COMMENT '文件名',
    `file_type`     VARCHAR(20)   NOT NULL                    COMMENT '文件类型',
    `file_size`     BIGINT        NOT NULL                    COMMENT '文件大小',
    `file_path`     VARCHAR(500)  NOT NULL                    COMMENT '存储路径',
    `status`        TINYINT       DEFAULT 0                   COMMENT '状态0待处理1已完成2失败',
    `error_msg`     VARCHAR(500)  DEFAULT NULL                COMMENT '失败原因',
    `create_time`   DATETIME      DEFAULT CURRENT_TIMESTAMP   COMMENT '上传时间',
    `update_time`   DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_knowledge_id` (`knowledge_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文档表';
```
